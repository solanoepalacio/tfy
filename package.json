{
  "name": "focus-filter",
  "version": "0.1.0",
  "type": "commonjs",
  "scripts": {
    "validate-youtube-fetch": "NODE_OPTIONS=--require ts-node/register/transpile-only node scripts/validate-youtube-fetch.ts",
    "metadata-cache-config": "NODE_OPTIONS=--require ts-node/register/transpile-only node scripts/metadata-cache-config.ts"
  },
  "dependencies": {
    "better-sqlite3": "^8.4.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.0"
  }
}
